<%= form_for(current_user.favorites.build, remote: true) do |f| %>
  <div><%= hidden_field_tag :movie_id, movie.id %></div>
<%= button_tag(type: "submit", class: "btn", name: "Favorite", value: "conceal") do %>
  <span class="glyphicon glyphicon-heart-empty" style="color:red"></span>
<% end %> 
<% end %>